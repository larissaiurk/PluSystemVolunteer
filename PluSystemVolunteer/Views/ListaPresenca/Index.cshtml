@model IEnumerable<PluSystemVolunteer.Models.ListaPresencaEvento>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ListaPresencaId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ListaPresencaId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ListaPresencaId })
            </td>
        </tr>
    }
    
    </table>
