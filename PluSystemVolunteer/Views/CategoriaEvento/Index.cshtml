@model IEnumerable<PluSystemVolunteer.Models.CategoriaEvento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoriaEventoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoriaEventoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoriaEventoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoriaEventoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoriaEventoId })
        </td>
    </tr>
}

</table>
